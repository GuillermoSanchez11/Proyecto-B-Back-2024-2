services:
  springboot-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8025:8025"
    networks:
      - my_bridge_network

networks:
    my_bridge_network:
      external: true